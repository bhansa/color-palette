// Component containing Heading and other values.
import React from 'react';
import classes from './Cockpit.css';

const cockpit = (props) => {
    return (
        <div className={classes.heading}>My Color Palette</div>
    );
}

export default cockpit;